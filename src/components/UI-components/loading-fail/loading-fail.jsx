import React from 'react';

const messageStyles = {
  color: `#eee5b5`
};

const LoadingFail = () => {
  return <h2 style={messageStyles} >Something went wrong... loading has failed.</h2>;
};

export default LoadingFail;
